import { SortColumnsPipe } from './sort-columns.pipe';

describe('SortColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
